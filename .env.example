# Copie para .env e preencha. O .env NAO vai para o git.
#
# Cada ${VARIAVEL} usada em config/cameras.yaml precisa existir aqui: a carga
# da configuracao falha se faltar alguma, de proposito.

CAM01_SENHA=troque-me
CAM02_SENHA=troque-me

# Opcional: silencia o aviso de provider do onnxruntime-gpu compilado com
# TensorRT quando as bibliotecas do TRT nao estao instaladas.
# ORT_LOGGING_LEVEL=3
